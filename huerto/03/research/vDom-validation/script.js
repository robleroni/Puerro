/*
import { h } from '../../../../src/vdom/vdom';
import { mount } from '../../../../src/vdom/vdom';

const $body = document.querySelector('body');

const div = h('div')()(h('form')()(h('input')()()));

mount($body,div);
*/
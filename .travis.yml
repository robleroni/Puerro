language: node_js
node_js: 'node'
script: npm run build
after_script: sed -i /bundle.js/d' .gitignore # removes bundle.js
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN # Set in the settings page of your repository, as a secure variable
  on:
    branch: master
